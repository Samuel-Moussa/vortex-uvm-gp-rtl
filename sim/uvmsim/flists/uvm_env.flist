# UVM Environment Compilation File List
# Compilation order is critical - packages first, then interfaces, then components
#
# OWNERSHIP RULE (enforced here):
#   If a file is `included inside a package, it must NOT appear as a standalone
#   flist entry. The package owns it. Duplicate compilation causes `ifndef guards
#   to fire on the second pass, silently dropping the class body and making the
#   type invisible to other standalone compilation units.
#
#   vortex_test_pkg  owns: mem_model, vortex_base_sequence, vortex_virtual_sequence,
#                          vortex_functional_mem_vseq, vortex_base_test,
#                          vortex_sanity_test, vortex_smoke_test,
#                          functional_memory_test
#
#   vortex_env_pkg   owns: vortex_virtual_sequencer, rvvi_txn, rvvi_monitor,
#                          vortex_scoreboard, lockstep_scoreboard,
#                          vortex_coverage_collector, vortex_env

# Set UVM environment base directory
+incdir+$UVM_HOME
+incdir+$VORTEX_UVM_HOME/tb
+incdir+$VORTEX_UVM_HOME/uvm_env
+incdir+$VORTEX_UVM_HOME/uvm_env/agents/mem_agent
+incdir+$VORTEX_UVM_HOME/uvm_env/sequences
+incdir+$VORTEX_UVM_HOME/uvm_tests

# 1. UVM (compiled separately first, already done)

# 2. Config package
$VORTEX_UVM_HOME/uvm_env/vortex_config.sv

# 3. Interfaces
$VORTEX_UVM_HOME/tb/vortex_axi_if.sv
$VORTEX_UVM_HOME/tb/vortex_mem_if.sv
$VORTEX_UVM_HOME/tb/vortex_dcr_if.sv
$VORTEX_UVM_HOME/tb/vortex_status_if.sv
$VORTEX_UVM_HOME/tb/vortex_if.sv

# 4. Memory model package (standalone package; imported by multiple packages)
$VORTEX_UVM_HOME/tb/mem_model.sv

# Lockstep record type + gating flags (Phase A0; queues removed in A1(c)).
# MUST precede rvvi_if.sv and the probes (both import it) and vortex_env_pkg.
$VORTEX_UVM_HOME/uvm_env/lockstep_pkg.sv

# RVVI hand-off interface + registry (Phase A1(c)): each probe instantiates an
# rvvi_if and registers it; rvvi_monitor (owned by vortex_env_pkg) drains all
# registered vifs and publishes rvvi_txn on its analysis port to the lockstep
# scoreboard. MUST follow lockstep_pkg (struct) and precede the probes (they
# instantiate rvvi_if and call rvvi_registry_pkg::rvvi_register).
$VORTEX_UVM_HOME/tb/rvvi_if.sv

# White-box architectural instruction coverage (ARCH-COV / instr_class_cg / Warp/scheduler-state functional coverage)
$VORTEX_UVM_HOME/tb/vx_instr_probe.sv
$VORTEX_UVM_HOME/tb/vx_sched_probe.sv
$VORTEX_UVM_HOME/tb/vx_commit_probe.sv
$VORTEX_UVM_HOME/tb/vx_lsu_probe.sv
$VORTEX_UVM_HOME/tb/vx_cache_probe.sv

# 5. Agent packages (each self-contained)
$VORTEX_UVM_HOME/uvm_env/agents/mem_agent/mem_agent_pkg.sv
$VORTEX_UVM_HOME/uvm_env/agents/axi_agent/axi_agent_pkg.sv
$VORTEX_UVM_HOME/uvm_env/agents/dcr_agent/dcr_agent_pkg.sv
$VORTEX_UVM_HOME/uvm_env/agents/host_agent/host_agent_pkg.sv
$VORTEX_UVM_HOME/uvm_env/agents/status_agent/status_agent_pkg.sv

# 5b. B1 — DCR RAL. MUST follow dcr_agent_pkg (the adapter builds a
# dcr_transaction) and MUST precede both vx_dcr_probe.sv (the probe calls
# vortex_dcr_ral_pkg::vx_dcr_observer::note) and vortex_env_pkg.sv (the env
# instantiates the reg block + checker). This is why the probe does NOT sit with
# the other tb/ probes above — its package dependency lands later than theirs.
$VORTEX_UVM_HOME/uvm_env/ral/vortex_dcr_ral_pkg.sv
$VORTEX_UVM_HOME/tb/vx_dcr_probe.sv

# 6. Agent components (standalone, each imports its own pkg at the top)

# 7. Ref model
$VORTEX_UVM_HOME/uvm_env/ref_model/simx_pkg.sv

# 8. Env package (imports only — does NOT include any sv class files)
$VORTEX_UVM_HOME/uvm_env/vortex_env_pkg.sv

# 10. Test package — includes ALL test classes and shared sequences inside it.
#     Do NOT add standalone entries for anything this package already includes.
$VORTEX_UVM_HOME/uvm_tests/vortex_test_pkg.sv

# 11. TB top (last)
$VORTEX_UVM_HOME/tb/vortex_tb_top.sv
