#include <math.h>

double dpi_fsqrt(double x) { return sqrt(x); }
double dpi_fdiv(double a, double b) { return a / b; }
double dpi_fmadd(double a, double b, double c) { return a * b + c; }

/* لو ظهرت رموز إضافية زي dpi_fmin أو dpi_fmax، أضفها هنا بنفس النمط */
